import css from 'styled-jsx/css'

const styles = css`
  .sticky-container {
    position: sticky;
    top: 0;
    z-index: 3;
  }

  .br-light {
    border-color: rgba(0, 0, 0, 0.1);
  }
`

export default styles
